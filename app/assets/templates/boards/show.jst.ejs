<h1><%= board.escape('title') %></h1>

<ul>
<% board.lists.each(function (list) { %>
  <li id="list_<%= list.id %>">
    <span class="list_entry" data-id="<%= list.id %>">
      <%= list.escape('title') %>
    </span>
  </li>
<% }) %>
<li>
<button class="newList">Add a List</button>
</ul>



<a href="#">Back to Boards</a>